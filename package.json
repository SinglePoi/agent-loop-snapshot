{
  "name": "agent-loop-snapshot",
  "version": "0.1.0",
  "private": true,
  "description": "Portable recording, inspection, visualization, and replay for agent loops.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=24.20.0 <25",
    "pnpm": ">=11.19.0 <12"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{js,json,ts,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{js,json,ts,yml,yaml}\"",
    "benchmark:trace": "pnpm build && node packages/trace/benchmarks/loader-100k.js",
    "benchmark:recorder": "pnpm build && node packages/recorder/benchmarks/recorder-10k.js",
    "benchmark:graph": "pnpm build && node packages/graph/benchmarks/project-100k.js",
    "benchmark:als-502": "pnpm benchmark:recorder && pnpm benchmark:trace && pnpm benchmark:graph",
    "pack:check": "pnpm build && pnpm -r --filter \"./packages/*\" pack --dry-run",
    "alsnap": "node packages/cli/dist/index.js",
    "test": "pnpm build && node --test scripts/release-smoke.test.js packages/schema/dist/index.test.js packages/recorder/dist/index.test.js packages/recorder/dist/writer.test.js packages/recorder/dist/artifacts.test.js packages/recorder/dist/checkpoints.test.js packages/recorder/dist/redaction.test.js packages/instrumentation/dist/index.test.js packages/instrumentation/dist/instrument.test.js packages/trace/dist/index.test.js packages/graph/dist/index.test.js packages/replay/dist/index.test.js packages/cli/dist/index.test.js packages/example-runtime/dist/index.test.js",
    "check": "pnpm typecheck && pnpm lint && pnpm format:check && pnpm test",
    "release:verify": "pnpm run check && pnpm run pack:check && node scripts/release-smoke.js",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.10.1",
    "eslint": "^9.39.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.2"
  }
}
